/*:
 ## Literales de arreglo
 
 Ya creaste cadenas, números y valores booleanos directamente en el código usando valores literales. También puedes crear un arreglo directamente en el código usando un _literal de arreglo_.

 Los literales de arreglo son listas de elementos que están separados por comas y encerrados entre corchetes:
*/
let devices = ["iPhone", "iPad", "iPod", "iMac"]
/*:
 Pasa el cursor por la lista de la barra lateral de resultados y usa el botón circular Show Result (Mostrar resultado) para agregar el arreglo al playground con la función inline. En la vista inline, puedes ver que cada elemento del arreglo tiene un número, a partir del cero, como se muestra a continuación:
 
`0 "iPhone"`\
`1 "iPad"`\
`2 "iPod"`\
`3 "iMac"`
 
 - experiment:(Experimento): \
Cambia el orden o el número de los elementos en el literal de arreglo `devices` (dispositivos) que aparece más arriba. Observa que el orden de los elementos en el visor de resultados siempre coincide con el orden en el que se agregaron al literal.
 
 
 - experiment:(Experimento): \
Crea una constante nueva llamada `highScores` (puntajes altos) a continuación y asígnale un arreglo que contenga una lista de diez números.
 */
// Definir el arreglo highScores a continuación
let highScores = ["a","b"]

/*:

 
 Avanza a la página siguiente para descubrir para qué sirven esos números.
 
[Anterior](@previous)  |  Página 2 de 17  |  [Siguiente: Índices](@next)
*/
